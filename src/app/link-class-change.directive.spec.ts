import { LinkClassChangeDirective } from './link-class-change.directive';

describe('LinkClassChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkClassChangeDirective();
    expect(directive).toBeTruthy();
  });
});
